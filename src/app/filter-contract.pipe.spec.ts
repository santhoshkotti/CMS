import { FilterContractPipe } from './filter-contract.pipe';

describe('FilterContractPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterContractPipe();
    expect(pipe).toBeTruthy();
  });
});
